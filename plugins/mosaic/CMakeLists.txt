add_library(downspout_mosaic_core STATIC src/mosaic_core.cpp)
target_link_libraries(downspout_mosaic_core PUBLIC downspout-project-options)
target_include_directories(downspout_mosaic_core PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include")
if(DOWNSPOUT_ENABLE_DPF AND DOWNSPOUT_DPF_DIR)
dpf_add_plugin(mosaic TARGETS ${DOWNSPOUT_DPF_PLUGIN_TARGETS} ${DOWNSPOUT_DPF_PLUGIN_ARGS} USE_FILE_BROWSER FILES_DSP src/dpf/MosaicPlugin.cpp FILES_UI src/dpf/MosaicUI.cpp)
target_link_libraries(mosaic-dsp PUBLIC downspout_mosaic_core)
target_link_libraries(mosaic-ui PUBLIC downspout_mosaic_core)
target_include_directories(mosaic PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include" "${CMAKE_CURRENT_SOURCE_DIR}/src/dpf")
target_include_directories(mosaic-ui PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include" "${CMAKE_CURRENT_SOURCE_DIR}/src/dpf")
install(DIRECTORY "${PROJECT_BINARY_DIR}/bin/mosaic.vst3" DESTINATION "." OPTIONAL)
endif()
if(BUILD_TESTING)
add_executable(downspout_mosaic_core_tests tests/mosaic_core_tests.cpp)
target_link_libraries(downspout_mosaic_core_tests PRIVATE downspout_mosaic_core)
add_test(NAME downspout_mosaic_core_tests COMMAND downspout_mosaic_core_tests)
endif()
