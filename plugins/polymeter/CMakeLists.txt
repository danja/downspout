add_library(downspout_polymeter_core STATIC src/polymeter_core.cpp)
target_link_libraries(downspout_polymeter_core PUBLIC downspout-project-options)
target_include_directories(downspout_polymeter_core PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include")
if(DOWNSPOUT_ENABLE_DPF AND DOWNSPOUT_DPF_DIR)
dpf_add_plugin(polymeter TARGETS ${DOWNSPOUT_DPF_PLUGIN_TARGETS} ${DOWNSPOUT_DPF_PLUGIN_ARGS} FILES_DSP src/dpf/PolymeterPlugin.cpp FILES_UI src/dpf/PolymeterUI.cpp)
target_link_libraries(polymeter-dsp PUBLIC downspout_polymeter_core)
target_link_libraries(polymeter-ui PUBLIC downspout_polymeter_core)
target_include_directories(polymeter PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include" "${CMAKE_CURRENT_SOURCE_DIR}/src/dpf")
target_include_directories(polymeter-ui PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include" "${CMAKE_CURRENT_SOURCE_DIR}/src/dpf")
install(DIRECTORY "${PROJECT_BINARY_DIR}/bin/polymeter.vst3" DESTINATION "." OPTIONAL)
endif()
if(BUILD_TESTING)
add_executable(downspout_polymeter_core_tests tests/polymeter_core_tests.cpp)
target_link_libraries(downspout_polymeter_core_tests PRIVATE downspout_polymeter_core)
add_test(NAME downspout_polymeter_core_tests COMMAND downspout_polymeter_core_tests)
endif()
