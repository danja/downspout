add_library(downspout_conductor_core STATIC src/conductor_core.cpp)
target_link_libraries(downspout_conductor_core PUBLIC downspout-project-options)
target_include_directories(downspout_conductor_core PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include")
if(DOWNSPOUT_ENABLE_DPF AND DOWNSPOUT_DPF_DIR)
dpf_add_plugin(conductor TARGETS ${DOWNSPOUT_DPF_PLUGIN_TARGETS} ${DOWNSPOUT_DPF_PLUGIN_ARGS} FILES_DSP src/dpf/ConductorPlugin.cpp FILES_UI src/dpf/ConductorUI.cpp)
target_link_libraries(conductor-dsp PUBLIC downspout_conductor_core)
target_link_libraries(conductor-ui PUBLIC downspout_conductor_core)
target_include_directories(conductor PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include" "${CMAKE_CURRENT_SOURCE_DIR}/src/dpf")
target_include_directories(conductor-ui PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include" "${CMAKE_CURRENT_SOURCE_DIR}/src/dpf")
install(DIRECTORY "${PROJECT_BINARY_DIR}/bin/conductor.vst3" DESTINATION "." OPTIONAL)
endif()
if(BUILD_TESTING)
add_executable(downspout_conductor_core_tests tests/conductor_core_tests.cpp)
target_link_libraries(downspout_conductor_core_tests PRIVATE downspout_conductor_core)
add_test(NAME downspout_conductor_core_tests COMMAND downspout_conductor_core_tests)
endif()
