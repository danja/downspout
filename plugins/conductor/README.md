# Conductor

Conductor emits bar-aligned section notes and scene, density, energy, mutation,
and reset CC values. It supports a fixed musical form or seeded weighted
section selection with bounded durations. It never changes the host graph.
