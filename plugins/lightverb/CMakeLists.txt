add_library(downspout_lightverb_core STATIC src/lightverb_core.cpp src/lightverb_serialization.cpp)
target_link_libraries(downspout_lightverb_core PUBLIC downspout-project-options)
target_include_directories(downspout_lightverb_core PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include")
if(DOWNSPOUT_ENABLE_DPF AND DOWNSPOUT_DPF_DIR)
  dpf_add_plugin(lightverb TARGETS ${DOWNSPOUT_DPF_PLUGIN_TARGETS} ${DOWNSPOUT_DPF_PLUGIN_ARGS} FILES_DSP src/dpf/LightverbPlugin.cpp FILES_UI src/dpf/LightverbUI.cpp)
  target_link_libraries(lightverb-dsp PUBLIC downspout_lightverb_core)
  target_link_libraries(lightverb-ui PUBLIC downspout_lightverb_core)
  target_include_directories(lightverb PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include" "${CMAKE_CURRENT_SOURCE_DIR}/src/dpf")
  target_include_directories(lightverb-ui PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include" "${CMAKE_CURRENT_SOURCE_DIR}/src/dpf")
  install(DIRECTORY "${PROJECT_BINARY_DIR}/bin/lightverb.vst3" DESTINATION "." OPTIONAL)
endif()
if(BUILD_TESTING)
  add_executable(downspout_lightverb_core_tests tests/lightverb_core_tests.cpp)
  target_link_libraries(downspout_lightverb_core_tests PRIVATE downspout_lightverb_core)
  add_test(NAME downspout_lightverb_core_tests COMMAND downspout_lightverb_core_tests)
endif()
