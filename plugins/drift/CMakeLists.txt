add_library(downspout_drift_core STATIC src/drift_core.cpp)
target_link_libraries(downspout_drift_core PUBLIC downspout-project-options)
target_include_directories(downspout_drift_core PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include")
if(DOWNSPOUT_ENABLE_DPF AND DOWNSPOUT_DPF_DIR)
dpf_add_plugin(drift TARGETS ${DOWNSPOUT_DPF_PLUGIN_TARGETS} ${DOWNSPOUT_DPF_PLUGIN_ARGS} FILES_DSP src/dpf/DriftPlugin.cpp FILES_UI src/dpf/DriftUI.cpp)
target_link_libraries(drift-dsp PUBLIC downspout_drift_core)
target_link_libraries(drift-ui PUBLIC downspout_drift_core)
target_include_directories(drift PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include" "${CMAKE_CURRENT_SOURCE_DIR}/src/dpf")
target_include_directories(drift-ui PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include" "${CMAKE_CURRENT_SOURCE_DIR}/src/dpf")
install(DIRECTORY "${PROJECT_BINARY_DIR}/bin/drift.vst3" DESTINATION "." OPTIONAL)
endif()
if(BUILD_TESTING)
add_executable(downspout_drift_core_tests tests/drift_core_tests.cpp)
target_link_libraries(downspout_drift_core_tests PRIVATE downspout_drift_core)
add_test(NAME downspout_drift_core_tests COMMAND downspout_drift_core_tests)
endif()
