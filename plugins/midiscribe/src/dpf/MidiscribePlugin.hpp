#pragma once

// MidiscribePlugin.hpp — included by the DPF build system to locate the DSP
// entry point.  The full class definition lives in MidiscribePlugin.cpp.

// This header intentionally contains no declarations; DPF discovers the plugin
// via createPlugin() which is defined in MidiscribePlugin.cpp.
