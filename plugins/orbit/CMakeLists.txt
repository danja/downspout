add_library(downspout_orbit_core STATIC src/orbit_core.cpp)
target_link_libraries(downspout_orbit_core PUBLIC downspout-project-options)
target_include_directories(downspout_orbit_core PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include")
if(DOWNSPOUT_ENABLE_DPF AND DOWNSPOUT_DPF_DIR)
dpf_add_plugin(orbit TARGETS ${DOWNSPOUT_DPF_PLUGIN_TARGETS} ${DOWNSPOUT_DPF_PLUGIN_ARGS} FILES_DSP src/dpf/OrbitPlugin.cpp FILES_UI src/dpf/OrbitUI.cpp)
target_link_libraries(orbit-dsp PUBLIC downspout_orbit_core)
target_link_libraries(orbit-ui PUBLIC downspout_orbit_core)
target_include_directories(orbit PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include" "${CMAKE_CURRENT_SOURCE_DIR}/src/dpf")
target_include_directories(orbit-ui PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include" "${CMAKE_CURRENT_SOURCE_DIR}/src/dpf")
install(DIRECTORY "${PROJECT_BINARY_DIR}/bin/orbit.vst3" DESTINATION "." OPTIONAL)
endif()
if(BUILD_TESTING)
add_executable(downspout_orbit_core_tests tests/orbit_core_tests.cpp)
target_link_libraries(downspout_orbit_core_tests PRIVATE downspout_orbit_core)
add_test(NAME downspout_orbit_core_tests COMMAND downspout_orbit_core_tests)
endif()
