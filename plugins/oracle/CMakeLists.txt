add_library(downspout_oracle_core STATIC src/oracle_core.cpp)
target_link_libraries(downspout_oracle_core PUBLIC downspout-project-options)
target_include_directories(downspout_oracle_core PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include")
if(DOWNSPOUT_ENABLE_DPF AND DOWNSPOUT_DPF_DIR)
dpf_add_plugin(oracle TARGETS ${DOWNSPOUT_DPF_PLUGIN_TARGETS} ${DOWNSPOUT_DPF_PLUGIN_ARGS} FILES_DSP src/dpf/OraclePlugin.cpp FILES_UI src/dpf/OracleUI.cpp)
target_link_libraries(oracle-dsp PUBLIC downspout_oracle_core)
target_link_libraries(oracle-ui PUBLIC downspout_oracle_core)
target_include_directories(oracle PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include" "${CMAKE_CURRENT_SOURCE_DIR}/src/dpf")
target_include_directories(oracle-ui PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" "${PROJECT_SOURCE_DIR}/plugins/generative-common/include" "${CMAKE_CURRENT_SOURCE_DIR}/src/dpf")
install(DIRECTORY "${PROJECT_BINARY_DIR}/bin/oracle.vst3" DESTINATION "." OPTIONAL)
endif()
if(BUILD_TESTING)
add_executable(downspout_oracle_core_tests tests/oracle_core_tests.cpp)
target_link_libraries(downspout_oracle_core_tests PRIVATE downspout_oracle_core)
add_test(NAME downspout_oracle_core_tests COMMAND downspout_oracle_core_tests)
endif()
