- [x] Add silent stereo compatibility outputs to the event-only plugins listed
  in `docs/bug-report_2026-07-27.md`.

- [x] Remove button-like format labels from BassGen, DrumGen, M-Mix, and P-Mix.
- [x] Show `v0.1.0` in those title subtitles, sourced from the same plugin-local
  constants as `getVersion()`. This intentionally follows the plugin metadata
  version rather than the repository release tag.
